"""Showcase rendering: auto-frame a camera on whatever was just generated
and produce a high-quality still, instead of requiring manual camera/light
setup to get a decent screenshot.

No bpy.types classes live in this package (the operator lives in
ui/operators.py); register()/unregister() are no-ops kept for the
subpackage registration contract.
"""


def register():
    pass


def unregister():
    pass
